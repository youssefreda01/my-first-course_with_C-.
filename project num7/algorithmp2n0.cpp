#include <iostream>
#include <cmath>
using namespace std;
int main()
    {
             cout << "calculate area\n";

             float A , B , D;
             cin >> A >> B >> D;

             cout << "area = " << A*sqrt((pow(D,2))-(pow(A,2)));

             return 0;
    }
